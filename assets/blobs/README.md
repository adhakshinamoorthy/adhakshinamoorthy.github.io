# Technology blob assets

The production blob cloud is CSS-driven in `assets/css/styles.css`, which keeps every blob responsive, theme-aware, animated, and keyboard focusable. Place exported SVG alternatives in this folder if a future topic needs a custom illustrated blob.
